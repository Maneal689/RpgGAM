//Exemple: 
//module.exports.startWith = startWith;

//function startWith(msg, test) {
    //if (typeof(msg) != "string" || typeof(test) != "string")
        //return (false);
    //if (test.length > msg.length)
        //return (false);
    //let i = 0;
    //while (i < test.length) {
        //if (test[i] !== msg[i])
            //return (false);
        //i++;
    //}
    //return (true);
//}
